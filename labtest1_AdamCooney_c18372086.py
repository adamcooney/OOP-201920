class MuseumMoney:

        myFile = open("MuseumsCollectionsArchives_2015.csv", "r")

        for line in myFile:
                print(line)

        thisDict = {
                myFile
        }

        def get_museum_area(self, amount):
                from queue import Queue
                lines = Queue(maxsize=600)

                if line.full():
                        print("line is full")

                for i in range(1, 600):
                        line.put(i)














